# 全局异常处理器设计文档

> 项目：Learning（Spring Boot 2.7.18）
> 相关类：`GlobalExceptionHandler`、`Result`、`BusinessException`（可选）
> 位置：`com.jiangpa.exception`

---

## 一、为什么要它

现在有两个问题都指向同一处缺失。

**一是错误响应的格式不统一。** 你在 DTO 上写了 `@NotBlank`、`@Size`，Controller 上也加了 `@Valid`，这些方向都对。但校验不通过时，Spring 抛的是 `MethodArgumentNotValidException`，返回的是它自带的那套 JSON：

```json
{
  "timestamp": "2026-09-13T10:30:00.000+00:00",
  "status": 400,
  "error": "Bad Request",
  "path": "/auth/register"
}
```

这跟你定义的 `{code, message, data}` 完全是两个结构。前端就得写两套解析逻辑：正常响应读 `code`，校验失败去读 `status` 和 `timestamp`，非常别扭。

**二是异常直接漏成 500。** 用户名唯一索引冲突时，数据库抛 `DuplicateKeyException`，没人接住就变成 500，用户看到的是"服务器内部错误"而不是"用户名已存在"。之前提过的"先查后插之间有竞态"，兜底方案就落在这里。

全局异常处理器的作用是：**给所有抛到 Controller 之外的异常做一次统一收口**，全部转成 `Result` 结构。

---

## 二、核心机制

用一个带 `@RestControllerAdvice` 的类，里面写若干 `@ExceptionHandler` 方法。Spring 在处理请求时如果抛了异常，会先找有没有匹配的处理器方法，找到就用它的返回值作为响应体，而不是走默认的错误页。

两个注解的分工：

- `@RestControllerAdvice` = `@ControllerAdvice` + `@ResponseBody`，作用在类上，声明"我要全局接管异常"，且返回值直接序列化成 JSON
- `@ExceptionHandler(XxxException.class)` 作用在方法上，声明"我负责处理这一类异常"

**匹配规则是找最具体的那个。** 比如同时存在 `@ExceptionHandler(BusinessException.class)` 和 `@ExceptionHandler(Exception.class)`，抛 `BusinessException` 时会走前者。所以兜底的 `Exception` 处理器永远写在最后，它只接住前面没人认领的。

---

## 三、需要覆盖的异常

按出现频率排序：

| 异常 | 触发场景 | 目标 code | 提示信息 |
| --- | --- | --- | --- |
| `MethodArgumentNotValidException` | `@RequestBody` 上的 `@Valid` 校验失败 | 400 | 取校验注解里写的 message |
| `ConstraintViolationException` | `@RequestParam`／`@PathVariable` 上的校验失败 | 400 | 同上 |
| `DuplicateKeyException` | 唯一索引冲突 | 400 | 用户名已存在 |
| `BusinessException` | 业务规则不通过（自定义） | 自定义 | 自定义 |
| `Exception` | 兜底 | 500 | 服务器开小差了，请稍后再试 |

前四条都有明确归因，最后一条是保险。**注意 `Exception` 兜底不能省，但也不能让它抢走前面几条的活**——顺序很重要。

关于第二条 `ConstraintViolationException`：这个容易被忽略。`@Valid` 只在 `@RequestBody` 上生效；如果你给 `@RequestParam` 或 `@PathVariable` 加校验注解（比如 `@Min(1)`），抛的是 `ConstraintViolationException`，需要额外在类上加 `@Validated` 才会被触发。你现在还没有这类用法，可以先放着，但知道有这回事，将来加了参数校验不至于查半天。

---

## 四、参数校验异常怎么取 message

`MethodArgumentNotValidException` 里可能带着多个字段的多个错误，需要把它取成一个字符串。

关键 API：

- `e.getBindingResult()` 拿到校验结果
- `.getFieldErrors()` 拿到所有字段级错误
- 每个 `FieldError` 上，`getDefaultMessage()` 就是你写在注解里的那句 `message = "用户名不能为空"`

常见的处理方式是拼成 `"用户名不能为空; 密码长度必须在 6-20 位之间"`。`getFieldErrors()` 返回的是 `List`，**要注意判空**——理论上校验失败时不会为空，但防御性写法更稳。

如果只想返回第一条，取 `list.get(0).getDefaultMessage()` 也行，前端提示更简洁。两种都可以，看你倾向。

---

## 五、数据库唯一索引冲突

`DuplicateKeyException` 来自 Spring 的 `org.springframework.dao` 包。这里有个细节值得知道：MyBatis 抛出的原始异常是 `SQLIntegrityConstraintViolationException`，Spring 的持久层异常转换器会把它**包装成** `DuplicateKeyException`。所以你要 catch 的是后者，不是前者——catch 错了接不住。

catch 之后直接返回 `Result.paramError("用户名已存在")` 即可。**不需要**再去分辨具体是哪个索引冲突了：一个表通常只有一个唯一索引有并发冲突风险，几个的话也可以按 `e.getMessage()` 里的索引名 `contains` 一下区分，但那是后话。

这条和 register 里的"先查后插"是配套的：查重是常态路径，给出友好提示；唯一索引是硬保障，负责堵住并发窗口。两者缺一不可——只有查重，并发下会插入两条重复数据；只有索引，用户看到的是 500。

---

## 六、业务异常（可选但推荐）

现在 Service 里判断出错时是直接 `return Result.paramError("...")`，能跑，但有个问题：**业务逻辑和响应格式耦合在一起了**。Service 本来只该关心"这单业务能不能办成"，不该关心"失败了该返 400 还是 404"。

更干净的做法是自定义一个 `BusinessException`，Service 里直接抛：

```java
public class BusinessException extends RuntimeException {
    private final Integer code;

    public BusinessException(String message) {
        this(400, message);
    }

    public BusinessException(Integer code, String message) {
        super(message);
        this.code = code;
    }

    // 省略 getter
}
```

为什么继承 `RuntimeException` 而不是 `Exception`：受检异常要求调用方显式 try-catch 或 throws，会让 Service 方法签名到处挂 `throws`，很啰嗦。业务异常属于"能预料的正常分支"，用运行时异常，交给全局处理器统一收口是主流做法。

这样 Service 写起来就是：

```java
User existUser = userMapper.selectOne(wrapper);
if (existUser != null) {
    throw new BusinessException("用户名已存在");
}
```

方法签名也可以从 `Result<?>` 改回 `UserVO`，返回值的语义更纯。**这是个重构，不是必须的**——你现在不改也能工作，只是架构上更清晰一点。建议先把前面几条做完、跑通了，再考虑要不要动。

---

## 七、类结构

位置：`com.jiangpa.exception.GlobalExceptionHandler`

```java
package com.jiangpa.exception;

@RestControllerAdvice
@Slf4j                     // 用 Lombok 的 @Slf4j 拿到 log 对象
public class GlobalExceptionHandler {

    // 1. @RequestBody 参数校验失败
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<?> handleValidException(MethodArgumentNotValidException e) { ... }

    // 2. 唯一索引冲突
    @ExceptionHandler(DuplicateKeyException.class)
    public Result<?> handleDuplicateKey(DuplicateKeyException e) { ... }

    // 3. 业务异常（如果做了第六节的重构）
    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusiness(BusinessException e) { ... }

    // 4. 兜底，必须放最后
    @ExceptionHandler(Exception.class)
    public Result<?> handleException(Exception e) { ... }
}
```

`@Slf4j` 是 Lombok 提供的，会自动生成一个 `private static final Logger log`，直接用 `log.error(...)` 就行，不用手写 `LoggerFactory.getLogger(...)`。你已经引了 Lombok，可以直接用。

import 清单（注意都是 `javax` 不是 `jakarta`，项目是 Boot 2.7）：

```java
import lombok.extern.slf4j.Slf4j;
import org.springframework.dao.DuplicateKeyException;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;
```

---

## 八、踩坑清单

1. **拦截器里的鉴权失败不走这里，但这是你的选择，不是限制。** 你在 `JwtInterceptor.preHandle` 里是直接往 `response` 写 401 的，那条路径不经过这个类——因为你根本没抛异常。

   需要澄清一个容易搞混的点：**`preHandle` 抛出的异常其实是能被这里接住的**。`DispatcherServlet.doDispatch()` 里，`applyPreHandle` 的调用被包在同一个 try 块内，所以它抛出的异常会跟 Controller 抛的一样，被交给 `HandlerExceptionResolver` 处理，`@RestControllerAdvice` 能捕获到。也就是说，你完全可以改成"拦截器里只 `throw new BusinessException(401, "未登录")`"，让响应格式只在这个类里定义一处，两种写法都成立。现在这样两处分别管一段也没问题，知道它们不冲突就行。

2. **兜底的 `Exception` 处理器不要写成返回 200。** 有些人为了让前端"不报错"把兜底也返回 200，这会让监控系统看不到任何异常，出故障时完全没告警。该 500 就 500。

3. **生产环境的兜底提示不要带原始异常信息。** `e.getMessage()` 可能包含 SQL 语句、表名、字段名，直接返回给前端等于把内部结构暴露了。日志里记全量堆栈，返回给用户的写一句通用的就够了。

4. **`@Valid` 不生效的常见原因**，遇到校验不触发时按这个顺序查：有没有在参数上加 `@Valid`；是不是 `@RequestBody`（`@RequestParam` 走的是另一套）；DTO 上有没有校验注解；`spring-boot-starter-validation` 在不在（你这已经有了）。

5. **别把 `Error` 也一起兜了。** `@ExceptionHandler(Exception.class)` 接不住 `OutOfMemoryError` 这类 `Error`，这是对的，不要去 catch `Throwable`。

---

## 九、自测方法

写完用 HTTP Client 或 Postman 验证四种情况：

1. 注册时 `username` 传 2 个字符 → 期望 400，`message` 是你写在 `@Size` 里的那句
2. 注册时 `username` 传空 → 期望 400，`message` 是 `@NotBlank` 的那句
3. 注册一个已存在的用户名 → 期望 400，`message` 是"用户名已存在"（注意这条会先被 register 里的查重拦下，想验证 `DuplicateKeyException` 那条分支，得并发发两个相同用户名的请求，或者临时注释掉查重逻辑）
4. 随便构造一个触发 500 的场景（比如临时在某个方法里写 `int i = 1 / 0;`）→ 期望 500，`message` 是通用提示，同时**控制台要能看到完整堆栈**

第 4 步的"控制台能看到堆栈"很重要——如果只返回了响应但日志里什么都没有，说明 `log.error` 漏了，线上出问题时你会两眼一抹黑。

---

## 十、这轮需要新增／改动

| 类型 | 内容 |
| --- | --- |
| 新增 | `GlobalExceptionHandler`（`com.jiangpa.exception`） |
| 新增 | `BusinessException`（`com.jiangpa.exception`，可选，见第六节） |
| 检查 | `GlobalExceptionHandler` 所在的包要在 `com.jiangpa` 之下，否则扫不到 |
